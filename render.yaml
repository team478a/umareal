services:
  - type: pserv
    name: umareal-api
    runtime: docker
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: pnpm --filter @keiba/api start
    preDeployCommand: pnpm db:migrate
    autoDeployTrigger: checksPass
    maxShutdownDelaySeconds: 30
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: umareal-db
          property: connectionString
      - key: DIRECT_DATABASE_URL
        fromDatabase:
          name: umareal-db
          property: connectionString
      - key: AUTH_PROVIDER
        value: supabase
      - key: APP_BASE_URL
        sync: false
      - key: ADMIN_BASE_URL
        sync: false
      - key: ENCRYPTION_KEY
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: NOTIFICATION_TRANSPORT
        value: line
      - key: LINE_OAUTH_TRANSPORT
        value: line
      - key: BILLING_TRANSPORT
        value: stripe
      - key: STRIPE_LIVE_MODE
        value: "true"
      - key: MAIL_TRANSPORT
        value: resend
      - key: RESEND_API_KEY
        sync: false
      - key: MAIL_FROM
        sync: false
      - key: JOB_SECRET
        sync: false
      - key: SENTRY_DSN
        sync: false
      - key: AUTH_RATE_LIMIT
        value: 60
      - key: CORRECTION_POLICY
        value: ADMIN_ONLY
      - key: DELAYED_PUBLICATION_POLICY
        value: CLOSED

  - type: web
    name: umareal-web
    runtime: docker
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: sh -c 'pnpm --filter @keiba/web exec next start --hostname 0.0.0.0 --port ${PORT:-10000}'
    healthCheckPath: /health
    autoDeployTrigger: checksPass
    maxShutdownDelaySeconds: 30
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_BASE_URL
        fromService:
          type: pserv
          name: umareal-api
          property: hostport

  - type: worker
    name: umareal-worker
    runtime: docker
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    dockerCommand: pnpm --filter @keiba/worker start
    autoDeployTrigger: checksPass
    maxShutdownDelaySeconds: 30
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: umareal-db
          property: connectionString
      - key: APP_BASE_URL
        sync: false
      - key: ENCRYPTION_KEY
        sync: false
      - key: NOTIFICATION_TRANSPORT
        value: line

databases:
  - name: umareal-db
    region: singapore
    databaseName: umareal
    user: umareal_app
    postgresMajorVersion: "16"
    ipAllowList: []
