# Phase 2 第2区間：事前評価とパドック入力

2026年9月12日。担当専門家がスマートフォンで事前評価と全頭のパドック評価を入力する機能を追加しました。本番サービスの完成や予想公開の実装ではありません。

## 実装した動作

- 担当レース一覧から評価画面を開き、馬番順に前後移動または直接選択。
- 事前点数0〜100、順位1〜18、印、短評を部分保存。
- 馬体の張り、歩様・踏み込み、毛艶、気合・集中力、発汗・落ち着きを1〜5または判断不能で入力。総合変化と短評、定型文候補を用意。
- 利用者＋レース単位で端末へ一時保存し、自動同期。保存済み/送信中/未送信/競合、全頭進捗と未入力馬を固定表示。
- 別端末とのrevision競合を検出し、端末値とサーバー値を比較して明示的に採用。出走馬・担当・レース変更も再確認を要求。
- 現在値とは別に、各保存の内容・出走馬スナップショット・操作者・理由を追記。履歴の更新・削除・TRUNCATEをDBトリガーで拒否。
- 担当EXPERT+AAL2またはADMIN+AAL2のみをAPIで認可。クライアントから送られたロールは受理しない。

## 主なファイル

| 対象 | ファイル |
| --- | --- |
| 入力ルール | packages/domain/src/assessments.ts |
| DB | packages/db/prisma/schema.prisma、migrations/202609120005_assessments/migration.sql |
| API | apps/api/src/assessments.controller.ts |
| 画面 | apps/web/components/assessment-editor.tsx、media-app.tsx、app/globals.css |
| 検証 | packages/domain/src/assessments.test.ts、tests/assessments.integration.test.ts、tests/e2e/assessments.spec.ts |

## セキュリティとデータ保護

権限、担当、MFA、出走馬ID、レースrevision、評価revisionをサーバーで再確認します。正常保存では現在値・追記履歴・監査・冪等キーを同じDBトランザクションで確定します。評価済み馬を別の馬IDへ入れ替える管理API/CSV操作も拒否します。

localStorageには評価内容が平文で残るため、共有端末の利用を避ける案内を表示しています。本番前に端末保存の保持期間、消去導線、端末管理方針を決定します。秘密情報、認証トークン、決済情報は保存しません。

## 検証結果

ローカルPostgreSQL 16.14とビルド済みWeb/APIで確認しました。専用テストは架空の会員・馬・将来日のレースを追加し、既存データを削除しません。

| 確認 | 結果 |
| --- | --- |
| Prisma生成・マイグレーション | 成功。全5件適用、最新状態 |
| typecheck / lint | 全パッケージ成功、警告なし |
| 単体テスト | 11件成功。未入力と判断不能、0点、範囲、既存権限・CSVを検証 |
| DB/API結合 | 13件成功。担当/MFA、部分保存、再送、同時競合、レース変更、履歴保護と既存機能を検証 |
| E2E | 10件成功。デスクトップ/モバイル各5件。オフライン保存・復帰同期・競合解決・進捗・履歴と既存機能を検証 |
| build | 全パッケージ成功。最終API変更も再ビルド成功 |
| 本番起動ガード | local認証のproduction起動拒否を確認 |

モバイル画像で1頭表示、固定進捗、入力ボタン、前後移動、変更履歴を確認しました。主操作は44px以上で、ページ全体の横方向のはみ出しもありません。GitHub Actionsは同じ検証群を実行する設定ですが、リモートへのpushやGitHub上の実行はしていません。.envと.localはGit管理外です。

## 次区間

最終予想、買い目、見送り、公開プレビュー、発走前締切、追記専用公開版、訂正を実装します。公開後の本文・印・買い目をDBで更新・削除不能にし、通知イベントを公開と同じトランザクションへ追加する必要があります。訂正承認者と延期後に締切を再開する条件はDECISIONS.mdの保留事項です。
