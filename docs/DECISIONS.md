# 設計判断と保留事項

## 採用済み

1. 初回はPhase 0 / Phase 1基盤。Next.js、NestJS、PostgreSQL、Prisma、pnpm workspaceを採用。別のホスティング用スターターへ置換しない。Sites公開は行わない。
2. ローカル認証で機能を検証し、Supabase向け署名検証アダプターを併設。localモードは本番起動不可、ループバックに限定。実環境に接続するまでSupabaseの完全動作を完了扱いにしない。
3. セッション8時間、リセット15分は開発用の技術既定値。セッションはDB保存、トークンは256bit乱数でDBへはSHA-256ハッシュのみ。パスワードはsalt付きscrypt。TOTP秘密はAES-256-GCMで暗号化。
4. 管理者・専門家の操作にはAAL2が必要。MFA成功時はセッションを入れ替え、使用済み時刻ステップをDBで拒否。一般会員の登録は必ずMEMBER。ユーザーのメタデータによる権限昇格は禁止。
5. APIは同意・権限・操作対象を検証。Next.jsは同一OriginのAPI中継。変更系はOrigin照合。API URLをブラウザーから指定させない。
6. 閲覧権限を課金とは独立した有限期間モデルにする。開始は包含、終了は非包含。1日利用はJSTの当日0時から翌日0時まで。手動付与には期間・理由・実行者を必須にし監査する。
7. ローカル文書はdraft-v1。正式な規約同意として移行しない。
8. 初回には予想の公開APIとテーブルを作らない。Phase 2で公開版、子データ、DB保護を一括導入する。監査ログには今回からUPDATE/DELETE/TRUNCATE拒否トリガーを適用する。
9. 予想公開と通知イベントの保存はPhase 2で同一DBトランザクションにする。送信はPhase 3ワーカー。トランザクション成功後にイベントを外部キューへ転送する構成で、公開直後のプロセス停止による通知欠落を防ぐ。
10. 初期技術バージョンはlockfileで固定。Prisma 6系列は既知のPostgreSQL構成に合わせ採用。メジャー更新は別変更で検証。

## 次フェーズ前に確定する事項

### Phase 2 第1区間（追加承認済み）

「次のタスク」で提示した開催日・レース・出走馬・担当者管理とCSV取込について、ユーザーの「進めてください」により実装を開始。予想入力・公開は次区間とし、本番公開・決済・LINE接続は含めない。

- レースは開催日＋競馬場＋番号、出走馬はレース＋馬番で照合。馬IDはUUID。同じ馬は開催をまたいで同じIDを使う。正式提供元のID対応は未確定。
- 手動の新規登録は重複を拒否。更新はrevision照合と理由が必須。レースの照合キーと登録済み馬番の変更は受け付けない。
- CSVはUTF-8、最大200データ行。完全な列を指定し、空の担当者は未割当、空のオッズ・人気は未確認として扱う。省略された行は削除しない。
- 取込は15分有効の操作者専用プレビューをDB保存し、確定時に権限・担当者の有効性・元データを再検証。管理トランザクションをPostgreSQL advisory lockで直列化し、全件成功か全件取消にする。直接DBを書く将来の処理も同じ排他契約が必要。
- 既存Phase 1レースを保持するため追加属性はDB上nullable。新規API/CSVでは必須検証。馬名は出走時のスナップショットを保持し、訂正が他レースの馬名に波及しない。
- 発走時刻変更は監査に記録する。公開締切や通知予定の再計算は公開・通知モデルを導入する次区間で実装し、延期時の再開方針は引き続き保留。

### Phase 2 第2区間（追加承認済み）

レース管理完了後の「次は事前評価とスマートフォン向けパドック入力」という提示に対するユーザーの「進めてください」により、この区間を実装対象とした。最終予想・公開版・訂正は次区間、本番公開・決済・LINE接続は含めない。

- 事前点数は0〜100、順位は1〜18。事前印は印なし/本命/対抗/単穴/連下/穴/危険馬を技術上の初期選択肢とする。公開時の印の個数制約は最終予想区間で決める。
- パドック5項目は1〜5に加えて「判断不能」を0で構造化する。nullは未入力、0は入力済みとして区別する。
- 馬ごとの部分入力を許可し、ブラウザーのlocalStorageへ利用者＋レース単位で一時保存して650ms後に同期する。端末共有は想定しない。本番前に保存データの端末方針と保持期間を決める。
- 更新はraceRevision、horseId、評価revisionで競合検出。競合時は端末入力を保持し、サーバーの最新値と比較して明示的に採用・再送する。各正常保存は履歴へ追記し、履歴はDBトリガーで更新・削除・TRUNCATEを拒否する。
- 全頭進捗はパドック5項目と総合変化が埋まれば完了。「判断不能」は完了に含む。事前評価の完了は点数・順位・印の入力。短評は任意、最大1000文字。

### Phase 2 第3区間（追加承認済み）

第2区間完了後の「次は最終予想、見送り、買い目、公開プレビュー、発走締切、追記専用の公開版・訂正履歴」という提示に対するユーザーの「次のゴールへ進めてください」により実装対象とした。通知イベントは同一DBトランザクションで作るが、外部送信・結果・成績・課金・本番公開は含めない。

- 下書きは部分保存可能。公開時は公開範囲、信頼度、勝負判断、総評を必須とし、見送り以外は本命を1頭要求する。見送りは買い目なし・0円の正式判断として保存する。
- 買い目は券種、組合せ群、1点100円単位の金額、点数、総額を整数円で保存。無効馬番・取消/除外/中止馬・券種ごとの頭数・同一組合せを公開前に拒否する。購入や自動生成は行わない。
- 公開前確認は15分有効で操作者本人だけが確定可能。確認後のレース、出走馬、担当、評価、下書き、公開版の変更を検知し、再確認を要求する。
- 公開版、凍結した印、買い目はDBトリガーで更新・削除・TRUNCATEを拒否。印・買い目の追加も公開版を作った同じDBトランザクションだけに限定する。訂正は前版IDと理由を持つ新しい版を追加する。
- 訂正承認は未確定の事業判断。開発既定値をADMIN_ONLYとし、検証用にEXPERT_OR_ADMINへ設定可能にした。本番開始前に確定する。
- 延期後の締切再開も未確定。開発既定値はCLOSED。明示設定LATEST_STARTS_ATの場合だけ、管理者が記録付きで変更した最新発走時刻まで許可する。どちらの場合もDBは最新発走時刻以降のINSERTを拒否する。
- 公開と同一トランザクションで内容を含まない通知イベントをQUEUEDとして保存。送信・再試行・宛先判定はPhase 3。

### Phase 3A 管理設定（追加承認済み）

ユーザーの「LINE連携などは管理画面で設定できるようにしてほしい。他にも管理機能を充実させてください」により、外部送信より先に管理設定基盤を実装対象とした。LINEへの実送信、Webhook受付、結果・成績、課金、本番デプロイはこの区間に含めない。

- Messaging APIのChannel ID、Channel secret、Channel access tokenと、LINE LoginのChannel ID、Channel secret、Callback URLを分けて管理画面から設定する。秘密値は既存の暗号化鍵でAES-256-GCM暗号化し、API・画面・監査ログへ再表示しない。環境変数との二重管理は行わずDBを設定元とする。
- LINE通知、LINE Login、予想公開、CSV取込、新規購入を独立したフラグにする。実装済みの予想公開とCSV取込は、プレビュー時と確定時の両方で停止を強制する。
- 通知の最大試行回数と初回待機秒数を保存する。指数バックオフ、送信、疎通確認は次区間で実装する。
- 設定変更はADMIN+AAL2に限定し、OPERATORは秘密値を除く状態を閲覧できる。revision競合検出、理由、監査履歴を必須にする。
- 管理ダッシュボードに公開待ちレース、QUEUED通知件数、停止中機能の警告を追加する。

### Phase 3B 通知運用（追加承認済み）

ユーザーの「ゴールを決めてまとめられる機能はまとめて実装してください」により、公開outboxから管理画面での再送までを一つのローカル通知運用区間として実装対象とした。外部LINE APIへの接続は含めない。

- 公開イベントはワーカーが受信者単位の配送へ展開する。初版は「最終予想の公開」、訂正版は「変更」設定を使い、有料版は展開時と送信直前の両方で有限期間の閲覧権限と対象JST日を確認する。
- 冪等キーはイベント種別、対象レース、受信者、公開版番号から作り、DB一意制約と行ロックで多重ワーカーによる重複試行を防ぐ。送信本文や印・買い目はoutboxと配送履歴へ保存しない。
- 一時失敗は管理設定の初期待機秒数を基準に指数バックオフし、最大試行回数で失敗へ移す。恒久失敗は即時に失敗とする。停止中はイベントと配送を保留し、再開可能な状態を維持する。
- 配送試行は追記専用テーブルへ保存し、DBトリガーで更新・削除・TRUNCATEを拒否する。失敗配送はADMIN+AAL2またはOPERATORが理由付きで1回の強制試行を要求でき、操作を監査する。
- ローカルのtest transportだけを提供し、本番環境では起動を拒否する。テスト用subjectの結果規則は検証専用で、実LINE資格情報を復号・送信しない。

### Phase 3C LINE本接続準備（追加承認済み）

ユーザーの「準備に進めてください」により、外部通信を開始する前にローカルで完結する接続準備を実装対象とした。外部LINE API送信、公開Webhook、LINE LoginのOAuth Callbackは含めない。

- 通知文は共通ビルダーで生成し、イベント種別、開催日、競馬場、レース番号、レース名、版、公開区分、会員ページURLだけを含める。印、馬番、評価理由、買い目、金額は通知transportへ渡さない。
- LINEの `X-Line-Retry-Key` には永続化済み配送IDのUUIDを初回から使えるtransport契約とし、自動再試行でも同じ値を渡す。内部の業務冪等キーは別に保持する。LINE側の保持期間24時間を越える手動再送は重複可能性を管理画面で扱う必要があり、実送信区間で最終方針を決める。
- 会員ページURLはHTTPSを必須とし、ローカルのlocalhost/127.0.0.1だけHTTPを許可する。レース由来の改行は除去し、LINEテキスト上限を超える文面を拒否する。
- Messaging APIのWebhook署名は受信した生バイト列に対するHMAC-SHA256を定数時間比較する部品として用意する。JSON再シリアライズ後の検証はしない。公開受付は次区間でレート制限・冪等処理と一緒に導入する。
- 管理設定APIは、資格情報の有無に加え、暗号化値を復号可能か、会員ページURL、ワーカー、署名検証部品の準備状態を返す。秘密値と復号結果は返さない。外部疎通済みとは表示しない。

### Phase 3D LINE Messaging API接続（追加承認済み）

Phase 3C完了後の次区間提示に対するユーザーの「進めてw」により、Messaging APIの実送信transportとWebhook受付を実装対象とした。保存済み資格情報を使うライブ疎通、実会員への送信、LINE Loginはこの区間では実行しない。

- push APIは固定の公式HTTPSエンドポイントだけを使い、Bearer token、JSON本文、配送IDの `X-Line-Retry-Key` を送る。2xxと受理済みを示す409は成功、5xx・タイムアウト・通信断は一時失敗、その他4xxは恒久失敗として安全なコードだけを履歴へ残す。レスポンス本文は保存・ログ出力しない。
- LINEのリトライキー保持期間に合わせ、初回試行から24時間を越える自動・手動再送を拒否する。24時間後に別キーで再通知する事業判断は保留し、曖昧な送信結果からの重複を優先して防ぐ。
- WebhookはOrigin検証の例外とする代わりに、保存済みMessaging API Channel secretと未加工本文による署名検証を必須にする。最大100イベントを受け、`webhookEventId` のDB一意制約で再配信を冪等化する。
- follow/unfollowは連携済みLINE subjectにだけ反映し、通知不可時刻と最終Webhook時刻を保存する。時刻が古い再配信で状態を巻き戻さない。未知イベントは本文を保存せず種別とハッシュだけ追記する。
- Webhook履歴はDBで追記専用にし、管理画面へ最終受信、24時間件数、未照合件数、通知不可アカウント数を表示する。

### Phase 3E LINE Login・会員連携（追加承認済み）

Phase 3D完了後の次ゴールとしてLINE Login・会員連携を提示し、ユーザーの「進めてください」により実装対象とした。実LINE資格情報を使う疎通、本番公開、新規会員の自動作成は含めない。

- 既存会員がログイン中にLINEアカウントを連携する。LINEだけで未登録会員を自動作成せず、未連携subjectによるログインは拒否する。
- OAuthフローは10分有効・使い切りとし、ランダムなstateとnonce、PKCE S256を必須にする。DBにはstate/nonceのハッシュ、nonceとcode verifierのAES-256-GCM暗号文だけを保存する。
- WebログインのID tokenはChannel secretでHS256署名を検証し、issuer、audience、有効期限、nonce、subjectをサーバーで確認する。token endpointとauthorization endpointは公式の固定HTTPS URLだけを使う。
- 連携フローは開始会員IDに拘束し、別セッションでの完了を拒否する。同じLINE subjectを別会員へ再割当しない。監査にはsubjectそのものを残さずSHA-256ハッシュだけを記録する。
- 解除はline_accountsを削除せず、解除時刻と通知停止時刻を残す。解除後はLINEログイン、通知対象展開、送信直前確認、Webhook状態反映の対象外とする。再連携は同じ会員がOAuthをやり直した場合だけ解除状態を戻す。
- ローカルtest transportは本番起動時に拒否する。ライブ疎通と実アカウント試験は本番接続準備として別途実施する。

### Phase 3F レース結果・公開版別成績（追加承認済み）

Phase 3E完了後に次ゴールとして提示した「レース結果・払戻・予想成績集計」に対するユーザーの「進めてw」により実装対象とした。正式な結果データ提供元への接続、CSV取込、代表成績ポリシーの確定は含めない。

- 結果は発走後にADMIN+AAL2またはOPERATORが入力する。全出走馬の状態・着順・人気・確定単勝と、券種・組合せごとの100円あたり払戻を構造化する。
- 結果下書きはrevisionで競合を検出する。確定結果は版として追記し、確定版、公開予想版ごとの成績、買い目ごとの精算をDBトリガーで更新・削除・TRUNCATE・後付けINSERTから保護する。公式訂正は下書きを直して新しい結果版を追加する。
- 払戻は入力された公式値を使い、外れは払戻行なしとする。取消・除外馬を含む公開買い目は、100円あたり100円のREFUND行が明示されない限り確定を拒否する。レース中止は全買い目を返還し、成績対象外とする。
- 見送りとレース中止は的中率・回収率の分母から除外する。返還は返還額として分離保存し、回収額には含める。整数円だけで計算し、凍結済み公開買い目を使用する。
- `VERSION_AUDIT_V1` は各公開版を独立して検証する参考集計であり、公式な広告・代表成績ではない。各レースでは最新結果版だけを集計し、過去の結果版は再計算と監査用に保持する。
- 訂正版の代表版、買い目なしの非見送り予想を的中率分母へ含めるか、同着の詳細表示、UP好走・DOWN凡走の閾値は未確定のため、公開指標として確定しない。

### Phase 4A 料金・契約基盤（追加承認済み）

Phase 3F完了後に次ゴールとして提示した料金プラン・契約ライフサイクルに対するユーザーの「進めてください」により実装対象とした。外部決済、実請求、返金、本番接続は含めない。

- 価格は管理設定とし、仕様書初期案の創設会員1,980円、通常会員2,980円、1日利用980円を開発初期値にする。正式な販売価格ではない。
- 創設会員販売は既定で停止し、上限100は開発初期値。100/200の正式選択は保留する。全契約履歴を販売枠として数え、解約後に枠を再利用しない開発実装とした。枠再利用方針は本番前に決める。
- 月額期間はローカル試験で申込UTC時刻から暦1か月とし、存在しない翌月日は月末へ丸める。外部決済導入時はproviderの期間を正とする。
- 支払猶予は0日を開発初期値として0〜30日で管理可能にする。失敗・回復は管理者が理由付きで行うローカル試験で、正式な督促・失効運用ではない。
- 月額契約と1日利用は有限期間entitlementを同一トランザクションで付与する。解約予約は現在の権限を失効させず、次回更新停止を記録する。
- 支払試行と請求イベントは追記専用とし、DBで変更・削除を拒否する。test transportは本番起動を拒否し、外部transport未実装時は申込を行わない。
- 自動更新・満了ジョブ、クーポン、試用、プラン変更、返金、領収書、正式表示はPhase 4B以降の保留事項。

### Phase 5A 無料会員登録（追加承認済み）

ユーザーが示した「会員登録→予想レース告知→情報配信」「無料会員登録→有料化」の基本フローと、LPを別途制作する前提に対し、WebアプリとLINE活用の併用を提案した。ユーザーの「あなたの提案通り進めてください」により、無料登録完了までを実装対象とした。CMS、実LINE疎通、実メール疎通、外部決済、本番デプロイは含めない。

- 会員・同意・閲覧権限はWebアプリを正本とする。LINEは無料登録・ログイン・通知の主経路、メールは代替登録と復旧経路にする。
- LINE登録はOAuth検証後に15分有効・使い切りの登録grantを発行し、表示名、20歳以上確認、版付き利用規約・プライバシー同意を得てMEMBERを作る。既存LINE subjectは再登録せず既存会員へログインする。
- メール登録は30分有効の使い切り確認URLを開くまで未完了とし、セッション発行とメールログインを拒否する。確認再送はアカウント有無を応答で明かさない。
- LINE登録者は確認済みメールとパスワードを予備認証として追加する。有料申込とLINE連携解除には予備認証を必須にし、LINE喪失時にログイン不能となる状態を避ける。
- メールtransportはローカルファイルとResendを実装する。本番はResend以外を起動拒否するが、実ドメイン・資格情報での疎通は別作業とする。
- LPは別制作、CMSは事業成立後の拡張候補とし、Phase 5Aには含めない。

### Phase 5B 対象レース告知（追加承認済み）

ユーザーの「今日はスマートフォン作業なのでスマートフォンでも進めていける部分を進めてください」により、無料登録後の基本フローに必要な対象レース告知を、スマートフォンから運用できる区間として実装した。CMS、予約告知、実LINE送信、本番公開は含めない。

- 登録済みレースから定型文を生成する。自由文CMSを先行導入せず、告知理由は監査用として会員通知へ含めない。
- 告知は追記専用の版として保存し、DBで更新・削除を拒否する。誤りや追加案内は再告知版を追加して履歴を残す。
- 告知公開と無料会員向け通知event作成を同一トランザクションで行う。発走済み、終了、中止レースは告知を拒否する。
- 受信者は予想通知を希望するLINE連携済み無料会員とし、展開時と送信直前に通知設定・LINE状態を再確認する。
- 会員ホームにはレースごとの最新告知だけを表示する。過去版はDBと監査へ保持し、管理画面の再告知版数で確認する。

### Phase 5C 会員利用準備と有料化導線（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、無料登録後に対象レース告知を確実に受け取り、有料プランの内容確認へ進む会員導線を実装対象とした。外部決済、実請求、CMS、本番接続は含めない。

- マイページで無料登録、LINE通知、予備ログイン、有料閲覧の4状態を確認できるようにする。
- LINE受信状態はサーバーが保持する連携、ブロック、会員通知設定から判定し、クライアント申告を信用しない。
- 対象レース告知と最終予想公開は同じ予想通知設定で扱う。通知オフ時はマイページから明示的に再開できる。
- 未公開レースは通知設定へ、有料ロックされた予想は料金プランへ案内する。権限のない本文は引き続きAPIから返さない。
- 開発用申込はプラン選択後に料金、期間、対象日を確認する画面を挟む。外部決済導入時は正式条件と同意画面へ置き換える。

### Phase 5D 会員向けお知らせ履歴（追加承認済み）

ユーザーの「進めてください」により、LINEを見逃した場合もWebアプリから対象レース告知と予想公開を追える会員履歴を実装対象とした。

- Web履歴はLINE配送レコードに依存しない。メール登録会員やLINE未連携会員も対象レース告知と無料予想公開を確認できる。
- 有料予想イベントは現在有効な有限期間権限をAPIで確認して返す。履歴APIには予想本文、印、買い目、具体的評価理由を含めない。
- 既読は会員と通知イベントの組で保存し、再操作で既読時刻を変更しない。公開イベントやLINE配送履歴は変更しない。
- 権限終了後の過去有料予想・通知を継続表示するかは既存の過去予想閲覧方針と合わせて本番前に決める。現在は有効期間内だけ表示する。

### Phase 5E レース検索導線（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、配信された情報をWebアプリ内で探し直せるレース一覧を実装対象とした。

- 開催日、競馬場、対象レース告知、最終予想の公開有無で絞り込む。CMSやお気に入り馬は追加しない。
- 一覧APIはレース基本情報、最新告知、最新公開版の版番号・状態・公開範囲・公開時刻だけを返す。予想本文、印、買い目、信頼度は返さない。
- 有料公開は一覧で存在を示すが、本文の閲覧可否はレース詳細APIが引き続きサーバー側で判定する。画面の鍵表示も現在有効な有限期間権限を参照する。
- 信頼度による絞り込みは、有料予想の信頼度を無料会員へ漏らす可能性があるため今回追加しない。公開範囲ごとの表示方針を決めるまで保留する。

### Phase 5F 会員ホーム行動案内（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、無料登録、告知確認、有料化の各導線をログイン後ホームでつなぐ機能を実装対象とした。

- 未読お知らせ、LINE受信状態、有料閲覧状態をサーバー所有データから表示する。
- 次の操作は、未読確認、LINE設定、料金確認、公開中レース確認の優先順で1つ表示する。事業施策による優先順の最適化はアクセス解析導入後に判断する。
- 新規会員へ登録以前の通知イベントを大量の未読として表示しない。Webお知らせ履歴は会員作成時刻以降のイベントを対象にする。過去レースはレース一覧から確認できる。
- 未読件数はサイドメニューと、モバイルでも常時見える上部通知ベルに表示する。

### Phase 5G 会員転換ファネル（追加承認済み）

ユーザーの「進めてください」により、無料登録から有料化までの詰まりを管理画面で確認する機能を実装対象とした。外部解析サービス、広告計測、外部決済、CMSは含めない。

- 無料登録、本人確認、LINE受信準備、料金ページ閲覧、申込内容確認、有料化を段階として扱う。
- 料金閲覧と申込確認は会員本人の初回到達だけを専用テーブルへ追記し、会員と段階の組を一意にする。管理画面には個人別行動を出さず集計人数だけを返す。
- 有料化は手動権限付与ではなく、成功した支払試行を基準にする。現在の閲覧権限人数は従来どおり別指標として表示する。
- 直近30日ファネルは直近30日に登録した会員のコホート、全期間は全会員を対象にする。計測開始前の料金閲覧・申込確認は推測や補完をしない。

### Phase 5H 開催日運用ボード（追加承認済み）

ユーザーの「進めてください」により、実際の開催日に運営担当者が停止箇所を一画面で判断する機能を実装対象とした。実LINE疎通、外部決済、CMS、本番デプロイは含めない。

- 運用日はJST日付で選択し、担当者、出走馬、対象レース告知、パドック完了数、最終公開、通知配送、結果確定をレース単位で集計する。
- パドック完了は評価レコードの存在ではなく、既存の全項目入力済み判定を使う。クライアントから進捗値を受け取らない。
- 発走30分以内を期限接近、発走時刻以降を期限超過とする。延期時の公開再開条件は既存の保留方針を維持する。
- 管理者は評価・公開画面へレース指定で移動できる。運営担当は監視とレース・通知・結果運用を行い、専門家操作権限は追加しない。

### Phase 5I 開催日ライブ監視（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、開催中に古い運用情報を見続ける事故を減らす機能を実装対象とした。外部監視サービスや通知先追加は含めない。

- 運用ボードの自動更新間隔は30秒、鮮度警告は最後の正常取得から90秒を開発初期値とする。APIの公開反映30秒目標とLINE送信開始60秒目標を確認できる間隔として採用する。
- 非表示タブでは定期通信を止め、表示へ戻った時点で即時取得する。手動更新も提供する。
- 残り時間はAPI取得時の固定値を表示し続けず、レースのサーバー保存発走時刻と端末現在時刻から継続更新する。期限に関するサーバー警告は次回取得時に確定する。
- 日付変更や連続更新で古いAPI応答が後から到着しても、現在画面の状態を上書きしない。

### Phase 5J 開催日リハーサル（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、開催日の操作順と阻害要因を管理画面で事前確認できる機能を実装対象とした。実LINE疎通、人手による実機計測、本番デプロイは含めない。

- レース準備、対象レース告知、パドック評価、最終予想公開、通知確認、結果確定を固定の6段階とする。
- 各段階はサーバー所有の担当、出走馬、公開版、配送、結果データと運用設定から判定する。クライアントの自己申告で完了にしない。
- 結果確定は発走前には「発走後」とし、告知から通知確認まで完了していれば発走準備完了として扱う。
- 通知対象が存在せずイベントがSKIPPEDで完了した場合も正常な確認結果とする。失敗配送、公開停止、通知停止、LINE設定不足は要対応として表示する。
- リハーサル画面から公開や送信を自動実行しない。既存の確認・監査・締切・権限を維持したまま各管理画面へ移動する。

### Phase 5K 障害対応チェック（追加承認済み）

ユーザーの「進めてください」により、開催日の障害検知と復旧確認を管理画面にまとめる機能を実装対象とした。外部監視、外部連絡先、実際の案内送信、本番復旧操作は含めない。

- 予想公開・CSV取込・LINE通知の停止、LINE設定不足、通知失敗、60秒超の初回処理待ち、5分超の送信中、24時間以内の未照合Webhookを検知する。
- 状態は重大な停止・停滞があればINCIDENT、警告だけならDEGRADED、対象なしならNORMALとする。
- 失敗配送は自動で消さず、既存の失敗一覧から原因解消後に理由付きで再送する。5分超の送信leaseは既存ワーカーの回収対象であり、画面からDB状態を直接変更しない。
- 会員向け案内文は公開停止またはLINE遅延の状態から定型文を生成するが、自動送信しない。内部のエラーコードや資格情報を文面へ含めない。
- ADMINとOPERATORが閲覧できる。OPERATORは設定変更画面へ進めず、管理者への連絡を表示する。

### Phase 5L バックアップ・復元確認（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、本番接続前にデータを戻せることをローカルで実証する機能を実装対象とした。

- 対象は開発用Windows PostgreSQLだけとし、local認証、非production、`127.0.0.1:55432/keiba` を全て必須にする。
- `pg_dump` が同梱ランタイムにないため、DB停止中の物理ディレクトリコピーをバックアップ形式にする。復元検証は固定の隔離ルートと別ポート55433だけで行い、元DBを復元先にしない。
- 全ファイルSHA-256、主要5テーブル件数、適用済みマイグレーション、履歴保護トリガーを照合する。失敗時も元DBの再起動を試み、削除対象は事前検証した一時復元ディレクトリだけに限定する。
- 管理画面はADMIN+AAL2の読取り専用とし、バックアップ実行、資格情報、絶対パス、内部ログを公開しない。
- 開発用バックアップは未暗号化でGit管理外。本番の保存方式、暗号化、保持世代、自動化、RPO/RTO、復元責任者は基盤選定後に決める。

### Phase 5M 退会・個人情報管理（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、会員本人が利用を終了し、運営が保持記録を確認できる機能を実装対象とした。

- 退会は物理削除ではなく即時の利用停止とする。全セッション、通知、LINE、現在・将来の閲覧権限を一つのトランザクションで停止する。
- 有効な月額契約または1日利用がある間は退会を拒否する。外部決済・返金・残期間の扱いを自動決定しない。
- 設定済みのパスワードと確認文言を本人確認に使う。パスワードを持たないLINE会員は認証済みセッションと確認文言で処理する。
- 退会記録と監査ログは追記専用で保持する。公開・評価、支払・契約、同意履歴との関係も削除しない。
- 正式な保持期間と匿名化範囲が未決定のため `development-v1` を記録し、本番用の個人情報削除完了とは扱わない。

### Phase 5N 本番準備チェック（追加承認済み）

ユーザーの「次のゴールへ進めてください」により、本番接続前の不足項目を管理者がスマートフォンから確認できる読取り専用機能を実装対象とした。

- 判定はサーバーが把握できる設定の有無とローカル復元結果だけを根拠にし、資格情報の値、絶対パス、内部ログを返さない。
- 認証、HTTPS、LINE Messaging API、LINE Login、メール、外部決済、正式文書、個人情報保持、復元試験、本番バックアップ、外部監視、機能停止状態の12項目を表示する。
- ローカル復元試験は7日以内の成功を開発上の確認済み条件とする。この期間は本番のRPO/RTOや保持方針を決定するものではない。
- 外部決済はWebhookを含む実装がない間は設定値にかかわらず要対応とする。本番バックアップは外部基盤上の事実を自動確認できないため人による確認とする。
- 自動判定は本番公開の承認ではない。ライブ疎通、法務、契約、監視、復旧責任者を人が確認して公開判断する。

### Phase 6A Stripe Checkout接続基盤（追加承認済み）

ユーザーの「進めてください」により、前回提案した無料会員から有料会員への外部決済基盤を実装対象とした。ライブ資格情報、本番請求、本番公開は含めない。

- Stripe hosted Checkoutを使用し、カード情報をアプリのDBへ保存しない。月額はsubscription、1日利用はpaymentモードとする。
- Checkout作成だけでは権限を付けない。署名済み `checkout.session.completed` の申込ID、会員ID、種別、プラン、JPY金額、live/testモードをサーバー保存値と照合した後だけ契約、支払、有限期間権限を同一トランザクションで作成する。
- StripeイベントIDとCheckout Session IDを一意にし、並行・重複Webhookでも一度だけ反映する。Webhook受信履歴はDBトリガーで更新・削除を拒否する。
- 月額の初回権限期間はCheckout完了時に暫定作成し、Phase 6Bの初回Invoice同期でStripeの請求期間へ補正する。
- 正式価格、返金、クーポン、無料期間、特商法表示は保留のままとし、現行の開発初期値を本番条件として扱わない。

### Phase 6B Stripe継続課金同期（追加承認済み）

ユーザーの「次へ進めてください」により、Phase 6A完了時に次ゴールとして提示した月額更新、請求失敗・回復、解約・終了のWebhook同期を実装対象とした。ライブ資格情報、本番請求、本番公開は含めない。

- Stripe契約の期間は `invoice.paid` の明細期間を正とする。初回InvoiceはCheckoutで作成した支払を二重計上せず期間だけ補正し、更新Invoiceから成功支払を追記する。
- `invoice.payment_failed` でPAST_DUEへ移し、支払済み期間の終了から管理設定の日数だけ権限を保持する。同じPAST_DUE中の再失敗では猶予期限を延長しない。猶予日数は未確定のため既存の0〜30日設定を使い、本番値とは扱わない。
- `invoice.paid` による回復は新しい請求期間へ権限を更新する。`customer.subscription.deleted` は契約と権限を終了する。解約予約・解除は `customer.subscription.updated` から同期する。
- 外部決済モードでは管理画面のローカル失敗・回復操作を禁止し、署名済みWebhookだけがStripe契約状態を変更する。
- 返金、クーポン、無料期間、プラン変更、領収書、課金LINE通知は引き続き保留する。

### Phase 6C Stripe管理設定（追加承認済み）

ユーザーの「stripe情報などの設定を管理画面でできるようにしたい」により、Stripe資格情報とPrice IDの管理画面設定を実装対象とした。ライブ疎通、本番決済、本番公開は含めない。

- Secret keyとWebhook secretはLINE秘密値と同じAES-256-GCMで暗号化し、API、画面、監査ログへ値を返さない。Price IDとテスト／本番モードは管理画面で確認・変更できる。
- 管理画面にStripe設定が1項目でもあればDB設定一式を優先し、環境変数と混在させない。未設定環境では既存の環境変数を互換用フォールバックとする。
- Secret keyの接頭辞と動作モード、2つの秘密値、3つのPrice IDが揃わない場合は購入とWebhook処理を開始しない。
- `BILLING_TRANSPORT` と本番の `STRIPE_LIVE_MODE=true` は管理画面だけで実請求へ切り替わらないための配備時安全スイッチとして環境変数に残す。
- Stripe設定変更はADMIN+AAL2と理由を必須にし、revision競合を拒否する。OPERATORは秘密値を含まない状態だけ閲覧できる。

### Phase 6D LP無料会員向け配信（追加承認済み）

ユーザーが提示したLPと実装の齟齬確認後、「実装を進めてください」と依頼したため、LPが約束する登録特典、評価UP/DOWN各1頭、本人音声、レース後検証を実装対象とした。CMS、ライブ疎通、本番公開は含めない。

- 無料速報を最終予想のFREE版とは分離し、無料会員へ本命、対抗、穴馬、全頭評価、買い目を返さない。
- 発走前速報はUP/DOWN各1頭と理由、HTTPS音声URLを必須にする。発走後の公開は禁止する。
- レース後検証は発走前速報と確定結果の存在、発走時刻経過、検証コメントを必須にする。UP/DOWN内容は発走前の公開版からコピーし、後から差し替えない。
- 公開は理由、冪等キー、監査ログを必須とし、公開版をDBトリガーで追記専用にする。
- 登録特典はCMSではなく固定1枠のタイトル、説明、HTTPS動画URLとして管理する。
- 毎週1レースの選定は初期運用では手動とする。予約公開、動画アップロード、未配信アラートは保留する。

### Phase 6E 無料速報の音声入力（追加承認済み）

ユーザーの「音声入力をできるようにしてほしい」により、スマートフォンのマイク録音と音声ファイル選択を無料速報管理画面へ追加した。

- ブラウザのMediaRecorderで最大5分を目安に録音し、録音終了後に認証付きAPIへ直接保存して試聴できるようにする。既存の外部HTTPS URL入力も残す。
- 1ファイル8MBを上限とし、許可MIMEとファイルシグネチャをAPIで確認する。音声はPostgreSQLの追記専用アセットとして保存し、更新・削除・TRUNCATEをDBトリガーで拒否する。
- 公開前の内部音声は管理権限のある担当者だけ、公開版が参照する音声はログイン会員だけ取得できる。音声配信はRangeリクエストに対応する。
- 初期運用の少量配信を優先してDB保存を採用する。外部オブジェクトストレージ、変換、波形編集、文字起こしは利用量を確認してから追加する。

### Phase 6F 配信予約・アラート（追加承認済み）

ユーザーが残機能のうち「配信予約・公開忘れアラート・配信失敗通知」を次の実装候補として承認したため、一つの開催日運用機能として実装対象とした。

- 対象レース告知と発走前の無料速報を、発走時刻より前の日時に予約できる。レース後検証は確定結果を人が確認する必要があるため自動予約しない。
- 無料速報の予約は予約時点のdraft revisionを固定する。実行までに下書き、選択馬、予約者権限、レース状態が変わった場合は自動公開せずFAILEDにし、公開済み情報を変更しない。
- 同一レース・同一種別の有効な予約は1件に限定する。待機中は理由付きで取り消せる。予約作成、取消、成功、失敗を監査ログへ記録する。
- ワーカーはDBロックで予約を一度だけ取得し、公開版と通知eventを同じトランザクションで作成する。失敗した予約は自動で内容を推測・修正しない。
- 発走30分以内の未告知、下書きがある未公開無料速報、1分超の実行遅延、予約失敗、LINE配送失敗を管理画面のアラートとして30秒ごとに表示する。30分と1分は初期運用の表示基準で、公開締切ルールそのものではない。

### Phase 6G LP・キャンペーン流入計測（追加承認済み）

ユーザーの「次へ進めてください」を、無料会員募集の基本フローで次に必要な効果測定へ進む承認として実装対象とした。

- LPは別途制作する前提を維持し、登録URLの標準UTM値と紹介コードだけを受け取る。閲覧履歴を横断追跡する仕組みや外部解析サービスは追加しない。
- 初回流入はメール登録とLINE登録の両方で保存する。LINEはOAuth開始時の値をサーバー内で固定し、コールバック後にブラウザーから改変できないようにする。
- 会員ごとに初回流入を1件だけ保存し、PostgreSQLで更新・削除・TRUNCATEを拒否する。UTMのない登録は `direct` とする。
- 管理画面は直近30日を流入元・媒体・キャンペーン別に集計し、成功支払の有無を有料化として表示する。広告費、CAC、LTVは費用データの入力元を決めてから拡張する。

### Phase 6H キャンペーンURL発行・集計CSV（追加承認済み）

Phase 6G完了時に次候補として示したキャンペーンURL発行とCSV出力について、ユーザーの「進めてください」により実装対象とした。

- 管理者+AAL2だけがキャンペーンを作成・閲覧・CSV出力できる。作成時は一意コードと監査理由を必須にする。
- URLは保存済み項目とAPP_BASE_URLからサーバーが生成する。任意URL、外部転送先、追加クエリは受け付けない。
- 初期運用ではキャンペーンの削除・編集・費用管理を設けない。誤発行も監査可能な記録として残し、新しいコードで発行し直す。
- CSVは1〜365日の流入別集計だけを含み、会員ID、メール、表示名を含めない。数式として解釈される値を無害化する。
- 広告費、CAC、LTV、外部広告API連携は費用データの管理方法を決めてから追加する。

### Phase 6I 独自ドメイン公開準備（追加承認済み）

ユーザーの「独自ドメインを設定して公開する準備をしてほしい」により、実サービスや資格情報へ接続しない範囲で本番配備の構成を実装対象とした。Render上のWeb・非公開API・常駐ワーカー・PostgreSQLを初期候補とする。Renderアカウントへのリソース作成、課金プラン選択、DNS変更、ライブ資格情報の投入、本番公開はこの区間に含めない。

- 公開入口はWebの独自ドメイン1つとし、APIは同一OriginのNext.js中継からRender private network経由で接続する。API用の公開ドメインを増やさない。
- LINEとStripeのWebhookもWeb中継を使うため、署名ヘッダーと未加工本文をAPIへ転送する。任意の受信ヘッダーは転送しない。
- DockerイメージはNode.jsとpnpmのバージョンを固定し、非rootユーザーでWeb、API、ワーカーのいずれも起動できる共通成果物とする。
- Render BlueprintはSingaporeリージョン、外部接続を閉じたPostgreSQL、CI成功後の自動配備、配備前マイグレーション、Webの統合ヘルスチェックを定義する。料金プランは利用量とバックアップ要件を確認して選ぶため固定しない。
- `ENCRYPTION_KEY` はAPIとワーカーに同じ32-byte base64値を設定する。秘密値をBlueprint、Git、ログへ保存しない。
- Supabase JWT検証アダプターだけでは無料登録を本番利用できない。ブラウザー側のSupabase登録・ログイン・セッション更新・ログアウトと、サーバー側ユーザー作成・同意記録の結合を完了するまで、本番認証は管理画面でも人による確認扱いにする。
- 正式な利用規約・プライバシーポリシー、個人情報保持、DB権限分離、外部監視、暗号化バックアップの確認も公開前条件として維持する。

### Phase 6J Supabase無料会員認証（追加承認済み）

ユーザーが独自ドメイン公開準備の継続を依頼したため、公開ブロッカーだったSupabaseの無料会員登録、メール確認、ログイン、セッション更新、ログアウト、パスワード再設定を実装対象とした。ライブprojectへの接続、初回管理者の作成、本番公開は含めない。

- メール確認とパスワード再設定はPKCE S256を使用し、確認コードは同じブラウザーのHttpOnly Cookieに保存したverifierで一度だけ交換する。
- アクセストークンと更新トークンはHttpOnly・SameSite=Lax Cookieに保存し、本番ではSecureを必須にする。ブラウザーのlocalStorageへ認証トークンを保存しない。
- 同一Origin API中継は認証済みAPIが401を返した場合だけ更新トークンを一度交換し、元のリクエストを一度だけ再試行する。
- Supabase user IDを`authSubject`へ結合するが、ロールとAAL判定は署名済みJWTと自社DBのサーバー管理値から決める。user metadataのロールは使用しない。
- メール登録のDB制約は、ローカル`passwordHash`または外部`authSubject`のどちらかを必須にする。外部認証を示すsubjectがないメール会員は、従来どおりローカルpassword hashなしでは作成できない。
- 無料会員、20歳確認、規約・プライバシー同意、初回流入はSupabase signup成功後に自社DBへ同一トランザクションで保存する。同意履歴はPostgreSQLで更新・削除・TRUNCATEを拒否する。既存メールを隠すidentity-less応答から会員を新規作成しない。
- 現行処理はanon keyだけを使用する。service-role keyは必要な管理操作が承認・実装されるまで配備しない。
- Supabase本番SMTP、Redirect URL、CAPTCHAとスタッフMFA復旧は本番接続前の確認事項として残す。

### Phase 6K 初回管理者・Supabase MFA（追加承認済み）

ユーザーの公開準備継続依頼により、初回管理者の作成とSupabase TOTP MFAを実装対象とした。実アカウントの昇格、factor登録、復旧操作は本番接続時に人が確認して実行する。

- 初回管理者は通常の無料会員登録とメール確認を先に完了し、Supabase User UIDとメールを二人で照合する。一度だけ使えるCLIが同じ値を自社DBで再検証し、有効ADMINが0人の場合だけMEMBERから昇格する。
- CLIはDB advisory lockとSerializable transactionを使い、昇格と監査ログを同時に保存する。Supabase service-role key、user metadataのrole、恒久的なbootstrap secretは使用しない。
- SupabaseモードのTOTP secretはSupabase Authが保持する。自社DBには確認済みfactor IDだけを保存し、secretを保存しない。
- MFA登録とログイン後の再確認はSupabaseのEnroll、Challenge、Verify APIを使う。Verifyが返す新しいアクセストークンと更新トークンをHttpOnly Cookieへ入れ替え、署名検証済みJWTの`aal=aal2`だけを管理権限判定に採用する。
- factor解除、端末紛失時の本人確認、復旧担当者、二名承認は事業運用の決定が必要なため管理機能を追加しない。募集開始前に緊急手順を確定する。

### Phase 6L 法務文書の公開ゲート（追加承認済み）

ユーザーの公開準備継続依頼により、CMSを導入せずに初回の正式文書を反映できる版管理と本番起動ガードを実装対象とした。

- 利用規約とプライバシーポリシーは共通パッケージの1定義を正本とし、表示本文と登録同意バージョンの不一致を防ぐ。
- 本文、固有バージョン、`YYYY-MM-DD` の施行日、`PUBLISHED` 状態のいずれかが欠ける場合、productionのAPI起動を拒否する。管理画面の本番準備判定も同じ検査を使う。
- 現在の本文と `draft-v1` は開発用のままとし、正式文書を推測して作成しない。審査済み文書の支給後に本文・版・施行日・状態を同時に更新する。
- 既存会員への改定後再同意は、適用日と必須化方針の事業判断が必要なため初回募集の範囲に含めない。

### Phase 6M PostgreSQL実行権限の分離（追加承認済み）

法務文書の支給待ちの間に独立して進められる公開前ブロッカーとして、マイグレーション所有者とAPI・workerのDB実行ロールを分離する。

- runtimeロールはpublic schemaオブジェクトを所有せず、特権属性と他ロールのメンバーシップを持たない。テーブルCRUDとsequenceの利用・参照だけを付与し、schema作成、TRUNCATE、TRIGGER、REFERENCESは許可しない。
- マイグレーション所有者の接続は保護された一時実行に限定し、APIとworkerの常駐環境へ保存しない。このためRenderの自動pre-deploy migrationは使用しない。
- 将来の所有者作成テーブルにも同じruntime権限を付与するdefault privilegesを設定する。マイグレーション後は検証CLIと管理画面で再確認する。production APIは同じ実権限検査を起動時に必須とする。
- runtimeロールから監査履歴のUPDATEを実行し、追記専用トリガーが拒否し、runtimeロールがトリガーを無効化できないことをPostgreSQL統合試験で確認する。

### Phase 6N 無料会員募集モード（追加承認済み）

ユーザーが機能、サーバー準備、法務文書準備を分け、機能を優先順に進めるよう依頼したため、最初の機能ゴールとして無料会員だけを募集できる公開モードを実装対象とした。

- 公開モードは `FREE_REGISTRATION` と `FULL` の2つに限定する。本番では明示を必須とし、不明な値や機能とtransportの矛盾を起動時に拒否する。
- `FREE_REGISTRATION` はメール登録、認証、会員ページ、無料情報、Web内お知らせを提供する。LINE Login、LINE通知、Stripe購入は画面とAPIの両方で停止する。
- 無料募集モードのworkerは予約公開を処理し、LINE資格情報を読み込まず、通知eventを`SKIPPED`に確定して配送を作らない。これにより`FULL`切替後の過去通知送信を防ぎ、event自体はWeb内お知らせの根拠として保持する。
- `FULL` はLINE・Stripeの実装済み安全条件を維持し、ライブtransportが揃わない本番起動を拒否する。
- 有料会員が存在する環境を `FREE_REGISTRATION` へ戻す運用は対象外とする。初回募集から`FULL`への一方向切替を前提とし、既存契約を伴う停止手順は別途決定する。

### Phase 6O 新規会員登録の停止・再開（追加承認済み）

Phase 6N完了時に次ゴールとして示した新規会員登録の運用制御について、ユーザーの「進めてください」により実装対象とした。

- 管理者+AAL2だけが、管理画面から新規登録を停止・再開できる。変更理由、実行者、変更前後、時刻を既存の設定監査へ保存し、revisionが古い更新は拒否する。
- 停止時は500文字以内の会員向け案内を必須にし、公開設定APIと登録画面へ表示する。監査用の変更理由は公開しない。
- メール登録、LINE OAuthの新規登録開始、LINE登録確定をサーバー側で拒否する。画面を迂回した直接API呼出しでも会員を作成しない。
- 既存会員のメール・LINEログイン、メール確認、パスワード再設定は継続する。登録停止をサービス全体のメンテナンス停止には使わない。
- 登録作成トランザクションは設定行を共有ロックして判定する。停止操作が完了した後に開始する登録が古い状態で作成されないようにする。

### Phase 6P 無料会員向けメール通知（追加承認済み）

ユーザーが機能を優先順に進めるよう依頼したため、無料会員登録後の予想対象レース告知と情報配信に必要なメール通知を実装対象とした。ライブ送信と本番公開は含めない。

- 通知eventはWeb内お知らせ、LINE、メールで共有する。配送、冪等キー、展開完了時刻はチャネルごとに分離し、一方の停止や失敗で他方を誤処理しない。
- メール配信対象は、確認済みメールを持つ有効会員のうち、メール全体と該当カテゴリを購読している会員に限定する。有料公開は送信直前にも対象日の権限を確認する。
- 公開通知本文にはレースのメタデータと会員ページURLだけを含め、予想内容、印、買い目、金額は含めない。
- 既存eventのメール展開時刻は移行時に埋め、機能導入前の告知を一斉送信しない。導入後に停止中となった新規eventは、管理者が再開した後に処理する。
- 認証、メール確認、パスワード再設定等の取引メールは会員の公開通知停止に連動させない。

### Phase 6Q メール配信設定の管理画面化（追加承認済み）

ユーザーが次の機能タスクへ進むよう依頼したため、Phase 6Pの認証・公開通知メールで共用するResend設定を管理画面から変更できるようにする。ライブ資格情報の検証と本番公開は含めない。

- Resend API keyはAES-256-GCMで暗号化し、送信元とともにsystem_settingsへ保存する。API応答、監査履歴、ログには平文、暗号文、末尾文字を含めない。
- ADMIN+AAL2だけが変更でき、OPERATORは設定元、設定有無、復号可否、送信元有無、transport、未疎通状態だけを確認できる。更新は既存revisionと理由を必須にする。
- 管理設定のAPI keyまたは送信元のどちらか一つでも存在する場合は管理設定一式を優先し、環境変数で不足項目を補完しない。管理設定がない場合だけ環境変数を初回起動用に使う。
- APIの登録・確認・再設定メールとworkerの公開通知は同じ有効設定を実行時に読み、管理画面保存後はプロセス再起動なしで反映する。
- `MAIL_TRANSPORT=resend`では取引メールにも必要なため完全な有効設定を必須にする。本番APIとworkerも不完全なメール設定では起動・処理を拒否する。

### 保留一覧

| 期限 | 項目 | 現時点の扱い |
| --- | --- | --- |
| 本番前 | 訂正承認の要否と承認者 | 開発既定値ADMIN_ONLY。EXPERT_OR_ADMINへ切替可能。正式決定ではない |
| 本番前 | 発走延期で締切を再開する条件 | 開発既定値CLOSED。LATEST_STARTS_ATへ切替可能。正式決定ではない |
| 本番前 | 3分試験の正式な計測条件 | 18頭・全項目の自動操作は約8秒。実機・人手・通信条件を定めたリハーサルが必要 |
| Phase 3 | 初版・訂正版・事前予想の代表成績 | 全版を再計算可能に保存。代表値の採用規則は未確定 |
| Phase 3 | UP好走・DOWN凡走、同着、返還、見送り、分母ゼロ | 具体的な集計ルールとサンプルで合意 |
| 本番前 | 訂正版を含む代表成績の採用版 | VERSION_AUDIT_V1は各公開版を独立集計。広告等の公式値には使わない |
| Phase 4 | 過去予想の閲覧・1日利用終了後の閲覧 | 現在の権限判定基盤は有効期間内のみ。事業判断に合わせ拡張 |
| Phase 4A | 料金・創設枠・猶予 | 管理設定とローカル契約試験を実装。正式条件と外部請求は未実装 |
| 本番前 | 正式価格・創設枠・枠再利用・猶予・月額期間 | Phase 4Aの値と期間計算はローカル開発初期値。正式な販売条件ではない |
| Phase 4B | 返金・クーポン・試用・プラン変更・領収書 | Stripe Checkoutと継続課金同期はPhase 6A/6Bで実装。残項目は実請求前に事業・法務判断が必要 |
| 本番前 | 名称・専門家表記・掲載許可・レースデータ許諾・法務文書 | 未確定。実在の人物の経歴や許可を推測しない |
| 本番前 | Supabase接続とメール・MFA復旧・セッション更新 | 実プロジェクトで結合試験が必要 |
| 本番接続前 | メール送信ドメイン・Resend資格情報のライブ疎通 | transport実装済み。送信元ドメイン認証と実アドレス試験は未実施 |
| 本番前 | API/worker配置先・SLO・DB最小権限・ログ保管期間 | Render準備構成とDB権限分離CLIは実装済み。本番runtimeロール、SLO、ログ保管は本番接続時に確認 |
| 本番前 | DBバックアップの暗号化・保存先・保持世代・RPO/RTO・復元責任者 | Phase 5Lは未暗号化のローカル物理コピーと隔離復元確認のみ |
| 本番前 | 退会後の個人情報保持期間・匿名化範囲・再登録・開示削除請求 | Phase 5Mは利用停止と追記専用記録のみ。法務・決済基盤確定後に正式化 |
| 本番接続前 | LINE Messaging API資格情報のライブ疎通と実送信リハーサル | 実装は完了。送信先と時間を決め、管理者立会いで別途実施 |
| 本番接続前 | LINE Login資格情報のライブ疎通と実アカウント試験 | OAuth実装は完了。Callback URLと同一Provider構成を確認して別途実施 |

## Phase 6R: Resend配信失敗Webhook

- Resend Webhookは生のHTTP本文と`svix-id`、`svix-timestamp`、`svix-signature`をSvix公式ライブラリで検証する。signing secretはAPI keyと同じ暗号化方式で管理し、画面・API・監査詳細へ値を返さない。
- `email.bounced`、`email.complained`、`email.suppressed`は恒久的な配信不達・苦情・Provider抑止として、照合した会員の公開通知メールを自動停止する。アカウントや認証メール機能そのものは停止しない。
- `email.failed`は送信ドメイン、API key、利用上限など送信者側の原因も含むため、受信者単位の自動停止には使わない。`email.delivery_delayed`も一時状態として記録だけ行う。
- Resendは少なくとも一回の配送で順序も保証しないため、`svix-id`をDBで一意にし、Webhook履歴は追記専用にする。後着した成功系イベントによる自動解除は行わない。本人が受信可能な状態にしたことを確認後、AAL2管理者だけが理由付きで解除できる。解除時も本人の`emailEnabled`は無効のままにする。
- Webhook履歴にはProvider event ID、email ID、種別、時刻、宛先・照合・停止の件数と結果だけを保存する。Provider本文、件名、送信元、メール本文は保存しない。管理者は最近の集計と停止会員を通知運用画面で確認する。
- 本番`MAIL_TRANSPORT=resend`では送信用設定に加えWebhook signing secretも必須とする。workerには送信用設定だけを渡せるが、APIは署名付き受信まで準備できなければ起動を拒否する。

## Phase 6S: 無料会員オンボーディングファネル

ユーザーの「進めてください」により、無料会員募集後の離脱箇所を管理画面で判断できる機能を実装対象とした。外部解析サービス、匿名閲覧者の追跡、広告費管理、個別会員への自動連絡は含めない。

- 登録作成、本人確認、初回ログイン、LINE案内到達、LINE受信準備を一つの登録コホートとして集計する。初回ログインは認証成功時、LINE案内は会員本人のマイページ表示時にサーバーへ初回到達を記録する。
- 行動イベントは会員と種別の組を一意にし、PostgreSQLで更新、削除、TRUNCATEを拒否する。導入前の初回ログインやLINE案内到達は推測して補完しない。
- 管理者+AAL2だけが専用画面を閲覧できる。集計期間は1〜365日、流入元は保存済みfirst-touch値だけで絞り込む。APIと画面には会員ID、メール、表示名、個別行動時刻を出さない。
- 各段階は登録比、直前段階からの率、未到達人数を返す。LINEを提供しない無料会員募集モードではLINE段階がまだ計測対象外であることを明示する。
- 有料化は従来どおり成功した支払を参考値として併記する。段階間の順序をクライアント申告で補正せず、サーバー所有データの有無を集計する。

## Phase 6T: 無料会員の本人確認フォロー

ユーザーが無料登録を優先して機能実装を進めるよう依頼したため、Phase 6Sで見つけた本人確認離脱を運営が確認し、必要な再送を安全に行う機能を実装対象とした。ライブ資格情報の検証、自動追客、外部CRM連携は含めない。

- 管理者+AAL2だけが、確認未完了・有効・メール登録の無料会員を個別に確認できる。表示項目は表示名、メール、登録時刻、直近の確認メール発行時刻に限定し、token hashやpassword hashを返さない。
- 登録から30分以上を初期の要確認目安とする。確認リンク自体も30分有効なためであり、マーケティング上の離脱確定を意味しない。この値は募集開始後の到達率を見て再検討する。
- ローカル認証では管理者が理由付きで確認メールを再送できる。会員行をPostgreSQLでロックして確認状態と直近送信を再検証し、5分以内の重複送信を拒否する。再送成功は監査履歴へ追記する。
- Supabaseのメール確認はPKCE verifierを本人ブラウザーのHttpOnly Cookieに保持するため、管理者ブラウザーから代理再送しない。管理画面は`/verify-email`の案内だけを提供し、会員本人が自分のブラウザーでメールアドレスを入力して再送する。
- メールアドレスを案内URL、監査詳細、ログへ含めない。自動再送や一括送信は、頻度、同意、問い合わせ対応の事業方針が決まるまで実装しない。

## 制約と残課題

レート制限はAPIプロセス内のストア。複数インスタンス運用前に共有ストアへ移行する。MFA復旧や退会・個人情報削除と監査保持の分離も本番前の課題。DB所有者はトリガーを無効化できるため、Phase 6Mの権限分離を本番DBで実行し、検証に合格したruntime接続だけを常駐サービスへ設定する。

## 参照した公式資料

- [Next.js 導入と構成](https://nextjs.org/docs/app/getting-started/installation)
- [Supabase MFA](https://supabase.com/docs/guides/auth/auth-mfa)
- [Prisma 6のマイグレーション運用](https://www.prisma.io/docs/orm/v6/prisma-client/deployment/deploy-migrations-from-a-local-environment)
- [LINE Webhook署名検証](https://developers.line.biz/en/docs/messaging-api/verify-webhook-signature/)
- [LINE Channel access token](https://developers.line.biz/en/docs/basics/channel-access-token/)
- [LINE Login Web連携](https://developers.line.biz/en/docs/line-login/integrate-line-login/)
- [LINE Login PKCE](https://developers.line.biz/en/docs/line-login/integrate-pkce/)
- [LINE ID token](https://developers.line.biz/en/docs/line-login/verify-id-token/)
- [Resend Webhookの受信と再試行](https://resend.com/docs/webhooks/introduction)
- [Resend Webhook署名検証](https://resend.com/docs/webhooks/verify-webhooks-requests)
- [Resendメールイベント種別](https://resend.com/docs/webhooks/event-types)
